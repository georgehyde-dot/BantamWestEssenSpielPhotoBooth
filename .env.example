# Photo Booth Camera Application Configuration
# Copy this file to .env and adjust values as needed

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Camera Configuration
VIDEO_DEVICE=/dev/video0
VIDEO_WIDTH=1920
VIDEO_HEIGHT=1080
VIDEO_FORMAT=MJPG

# Storage Configuration
# Base directory for storing captured photos and templates
STORAGE_PATH=/usr/local/share/photo_booth

# Database Configuration
# SQLite database file path (defaults to STORAGE_PATH/photo_booth.db if not set)
DATABASE_PATH=/usr/local/share/photo_booth/photo_booth.db

# Printer Configuration
# Primary printer name (as shown in 'lpstat -p')
PRINTER_NAME=XP8700series-TurboPrint
# Comma-separated list of fallback printer names
PRINTER_FALLBACK=EPSON_XP_8700_Series_USB,XP-8700
# Set to true to use mock printer (for testing without physical printer)
USE_MOCK_PRINTER=false

# Template Configuration
# Text that appears on the print template
TEMPLATE_HEADER=Photo Booth
TEMPLATE_NAME=NAME HERE
TEMPLATE_HEADLINE=HEADLINE
TEMPLATE_STORY=STORY HERE
# Background image filename (relative to static directory)
TEMPLATE_BACKGROUND=background.png

# Logging Configuration
# Options: error, warn, info, debug, trace
RUST_LOG=info

# Additional logging for specific modules (examples)
# RUST_LOG=info,canon_test_cam=debug
# RUST_LOG=info,canon_test_cam::camera=debug,canon_test_cam::printers=debug
